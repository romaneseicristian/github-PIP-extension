// Minimal service worker — kept lightweight. We don't rely on background messaging for the core PiP trigger.
self.addEventListener('install', () => {
// noop
});


self.addEventListener('activate', () => {
// noop
});